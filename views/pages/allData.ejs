<%- include('../partials/header') %>

<main class="main" id=""allDataMain>
    <div class="container">
        <div class="row row-cols-1 row-cols-ms-3 row-cols-lg-4">
            <% data.forEach(item => { %>
                <div class="col">
                    <div class="card h-100">
                        <div class="card-body">
                            <h2 class="card-title card-heading">
                                
                            </h2>
                        </div>
                    </div>
                 </div>
            <% }) %>
        </div>
    </div>
</main>

<%- include('../partials/footer') %>
